<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="AppTheme" parent="Theme.MaterialComponents.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/blue</item>
        <item name="android:windowBackground">@android:color/white</item>
    </style>

    <style name="CustomTabLayout" parent="Widget.Design.TabLayout">
        <item name="tabBackground">@color/blue</item> <!-- Warna latar belakang tab -->
        <item name="tabTextColor">@android:color/white</item> <!-- Warna teks tab -->
        <item name="tabSelectedTextColor">@android:color/white</item> <!-- Warna teks tab yang dipilih -->
    </style>
</resources>
